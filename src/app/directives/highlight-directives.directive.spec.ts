import { HighlightDirectivesDirective } from './highlight-directives.directive';

describe('HighlightDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
